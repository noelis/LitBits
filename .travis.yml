language: python
python:
- "2.7"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run before tests
before_script: "python server.py"
# command to run tests
script: python tests.py